{
  "name": "ai-operator-os",
  "productName": "AI Operator OS",
  "version": "0.1.0-alpha",
  "description": "A local-first operating system for AI-powered businesses.",
  "author": {
    "name": "AI Operator"
  },
  "license": "UNLICENSED",
  "main": "electron/main.cjs",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "npm run build && concurrently -k \"vite build --watch --configLoader runner\" \"vite preview --configLoader runner --port 5173 --strictPort\" \"wait-on http://localhost:5173 && electron .\"",
    "build": "tsc --noEmit && vite build --configLoader runner",
    "preview": "vite preview --configLoader runner",
    "dist": "npm run build && electron-builder --win nsis portable --config electron-builder.json",
    "dist:installer": "npm run build && electron-builder --win nsis --config electron-builder.json",
    "dist:portable": "npm run build && electron-builder --win portable --config electron-builder.json"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-markdown": "^10.1.0",
    "react-router-dom": "^7.1.1",
    "recharts": "^3.9.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^18.3.18",
    "@types/react-dom": "^18.3.5",
    "@vitejs/plugin-react": "^4.3.4",
    "autoprefixer": "^10.4.20",
    "concurrently": "^9.1.2",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.17",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "wait-on": "^8.0.1"
  }
}
